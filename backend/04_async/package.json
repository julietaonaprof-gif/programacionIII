{
  "name": "04_sync",
  "version": "1.0.0",
  "main": "app.ts",
  "scripts": {
    "build": "npm run clean && tsc && cp -r src/data dist/",
    "clean:build": "rm -rf dist",
    "clean:temp": "rm -rf tmp",
    "clean": "npm run clean:build && npm run clean:temp",
    "start": "node ./dist/app.js",
    "start:dev": "tsc && node ./dist/app.js",
    "test": "npx jest tests --runInBand --detectOpenHandles"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/jest": "^30.0.0",
    "@types/sqlite3": "^3.1.11",
    "jest": "^30.3.0",
    "typescript": "^6.0.3"
  },
  "dependencies": { }
}
